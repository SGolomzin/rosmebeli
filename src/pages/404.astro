---
import Layout from "../layouts/BaseLayout.astro";
import Link from "../components/Link.astro";

import { baseUrl } from "../helpers/helpers";
---

<Layout title="404 | РОСМЕБЕЛЬ">
	<main>
		<section class="section-block">
			<div class="container">
				<span class="error-code">404</span>
				<h1>Страница не найдена</h1>
				<p>Похоже, такой страницы не существует, пожалуйста перейдите на главную</p>
				<Link href={baseUrl("./")} appearance="button-ghost">Перейти на главную</Link>
			</div>
		</section>
	</main>
</Layout>

<style lang="scss">
	.container {
		margin-top: 5rem;
		text-align: center;
	}

	.error-code {
		font-family: var(--font-family-secondary);
		font-size: 15.6rem;
		color: var(--clr-pastel-red);
	}

	h1 {
		margin-bottom: 0;
	}

	p {
		margin-bottom: 3rem;
	}
</style>